<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Calorium.Views.QRScanner"
             Title="QR Scanner"
             xmlns:cv="clr-namespace:Camera.MAUI;assembly=Camera.MAUI"
             BackgroundColor="White"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollView>
        <VerticalStackLayout>
            <zxing:CameraBarcodeReaderView x:Name="barcodeReader" IsDetecting="True" IsTorchOn="False" HeightRequest="300" WidthRequest="300" BarcodesDetected="CameraBarcodeReaderView_BarcodesDetected"/>
            <Label x:Name="barcodeResult" FontSize="30" TextColor="Black" VerticalOptions="End" Grid.Row="1"/>
        </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>